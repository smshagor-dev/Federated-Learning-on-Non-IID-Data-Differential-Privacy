name: ci release normalize

on:
  push:
    branches: [ci-release-stabilization]

permissions:
  contents: write

jobs:
  normalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install CI Ruff version
        run: python -m pip install 'ruff==0.16.4'
      - name: Apply safe fixes and canonical formatting
        run: |
          ruff check --fix . || true
          ruff format .
          git diff --check
      - name: Commit normalized files
        run: |
          if git diff --quiet; then
            echo 'No normalization changes required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Normalize current Python sources'
          git push origin HEAD:ci-release-stabilization
      - name: Verify post-format Ruff state
        run: ruff check .
