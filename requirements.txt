torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
seaborn>=0.12.0
PyYAML>=6.0
# Privacy Engineering phase: opacus backs real sample-level DP-SGD
# training (fl_platform.worker.task_runner); prometheus_client backs
# fl_platform.privacy.metrics, imported unconditionally by
# fl_platform.privacy so ANY worker (private or not) needs it just to
# start. See docs/known-limitations.md's Privacy Engineering Phase
# section.
opacus>=1.5.0
prometheus_client>=0.20.0
# Secure worker/coordinator gRPC path: tests and the optional real
# GrpcCoordinatorClient runtime import grpc directly.
grpcio>=1.76,<2
# Generated Python protobuf bindings import google.protobuf directly and
# currently target protobuf 7.35.1.
protobuf>=7.35.1,<8
# Secure Transport and Worker Identity Hardening slice: pynacl backs
# Ed25519 worker signing identities and capability-statement signing
# (fl_platform.security); cryptography backs X.509 certificate loading
# for mTLS channel construction. See docs/cryptographic-primitives.md.
pynacl>=1.5,<2
cryptography>=42,<50
